{
  "name": "webkit-normalize.css",
  "version": "1.1.0",
  "description": "A CSS resets, optimized for mobile browser(webkit engine) and local zh_CN specially.",
  "keywords": ["css reset", "css normalize", "webkit", "style"],
  "main": "normalize.css",
  "devDependencies": {
    "stylelint": "^7.9.0",
    "stylelint-config-standard": "^16.0.0"
  },
  "scripts": {
    "test": "stylelint normalize.css"
  },
  "repository": "git@github.com:rouberg/webkit-normalize.css.git",
  "license": "MIT",
  "bugs": "https://github.com/rouberg/webkit-normalize.css/issues",
  "stylelint": {
    "extends": "stylelint-config-standard"
  }
}
